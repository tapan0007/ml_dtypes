# this is terrible, need to find a better way, depends where package build & publish will be running.
# Could start build instance with an IAM build role providing necessary access
# Or if we want to run this from DevDesktop, we could use Odin
export AWS_ACCESS_KEY_ID=AKIAJSWWN3P2GQQAXUTA
export AWS_SECRET_ACCESS_KEY=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

